<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Lembrador.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Lembrador.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar 
    x:Name="Lembretes">
        <Tab Title="Lembradores" Icon="add_lembrete_icon.png">
            <ShellContent
            ContentTemplate="{DataTemplate local:Lembrador}"
            Route="Lembrador">
            </ShellContent>
        </Tab>
        <Tab Title="Previsão do Tempo" Icon="add_lembrete_icon.png">
            <ShellContent
            ContentTemplate="{DataTemplate local:TemperaturaPage}"
            Route="TemperaturaPage">
            </ShellContent>
        </Tab>
    </TabBar>

</Shell>
